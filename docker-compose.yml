services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      DB_PATH: /data/workshop.db
      PORT: 3000
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
